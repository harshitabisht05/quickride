name: QuickRide Database Keep Alive

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  keep-alive:
    runs-on: ubuntu-latest

    steps:
      - name: Ping QuickRide backend
        run: |
          curl -f https://quickride-kappa.vercel.app/health